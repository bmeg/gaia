syntax = "proto3";

package gaia.agent;

message KafkaHost {
  string host = 1;
  repeated string topics = 2;
}

message AgentConfig {
  repeated KafkaHost inputTopics = 1;
  repeated KafkaHost outputTopics = 2;
  repeated string inputMessages = 3;
  repeated string outputMessages = 4;
}